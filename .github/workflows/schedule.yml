name: Update gist with RescueTime stats
on:
  schedule:
    - cron: '*/10 * * * *'
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Update gist
        uses: joshghent/rescue-box@master
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: ${{ https://gist.github.com/Mona04/da6a0e9976371f5831a3f59b42f4d71f }}
          RESCUETIME_API_KEY: ${{ B63wKjCLN5od1RUnJ6FJaPDxxiqoY89WwM03BsQr }}
